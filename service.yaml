name: yolo-service
version: 0.1.0
description: Backend object detection + segmentation service using YOLOv8 on GPU

requires:
  gpu: true
  vram_gb: 4

deploy:
  image: tidybot/yolo-service:0.1.0
  port: 8000
  health: /health
  ready_timeout: 120

client: client.py
